import { CardlayoutDirective } from './cardlayout.directive';

describe('CardlayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new CardlayoutDirective();
    expect(directive).toBeTruthy();
  });
});
